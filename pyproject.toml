[project]
name = "voiceguard-ai"
version = "2.0.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.117.1",
    "numpy>=1.26.0",
    "scikit-learn>=1.3.2",
    "joblib>=1.3.2",
    "librosa>=0.10.1",
    "soundfile>=0.12.1",
]

[tool.vercel]
entrypoint = "api.index:app"
